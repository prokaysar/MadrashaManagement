<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.prokawsar.madrashamanagement">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".VortiActivity" />
        <activity android:name=".AllJamatActivity" />
        <activity android:name=".StudentNameListActivity" />
        <activity android:name=".StudentInfoActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".TeacherAddActivity" />
        <activity android:name=".CommiteeAddActivity" />
        <activity android:name=".AboutCommiteeActivity" />
        <activity android:name=".AboutTeacherActivity" />
        <activity android:name=".AddFancActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CostActivity"></activity>
    </application>

</manifest>